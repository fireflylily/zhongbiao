# 数据库同步操作手册

> **目标**: 将本地数据库同步到阿里云服务器
> **预计时间**: 5-10 分钟
> **难度**: ⭐⭐ (简单)

---

## 📋 准备工作

### 确认信息

- **阿里云IP**: 8.140.21.235
- **SSH用户**: lvhe
- **项目路径**: /var/www/ai-tender-system
- **数据库文件**: knowledge_base.db (包含所有业务数据)

---

## 🖥️ 第一部分：本机操作

### 步骤 1: 打开终端

在 Mac 上按 `Command + 空格`，输入 "终端" 或 "Terminal"，打开终端应用。

---

### 步骤 2: 进入项目目录

```bash
cd /Users/lvhe/Downloads/zhongbiao/zhongbiao
```

**验证**: 输入 `pwd` 确认当前路径是否正确，应该显示：
```
/Users/lvhe/Downloads/zhongbiao/zhongbiao
```

---

### 步骤 3: 查看本地数据库统计（可选）

```bash
sqlite3 ai_tender_system/data/knowledge_base.db << 'SQL'
.mode column
.headers on
SELECT
    (SELECT COUNT(*) FROM companies) as 公司数,
    (SELECT COUNT(*) FROM documents) as 文档数,
    (SELECT COUNT(*) FROM resumes) as 简历数,
    (SELECT COUNT(*) FROM case_studies) as 案例数;
SQL
```

**预期输出**:
```
公司数      文档数      简历数      案例数
----------  ----------  ----------  ----------
2           0           1           1
```

**说明**: 这是你本地的数据，等会会同步到阿里云。

---

### 步骤 4: 检查数据库文件大小

```bash
ls -lh ai_tender_system/data/knowledge_base.db
```

**预期输出**:
```
-rw-r--r--@ 1 lvhe  staff   744K 11  3 10:40 ai_tender_system/data/knowledge_base.db
```

**说明**: 确认文件存在且大小约为 744KB。

---

### 步骤 5: 测试 SSH 连接（重要）

```bash
ssh lvhe@8.140.21.235 "echo '✓ SSH连接成功'"
```

**预期输出**:
```
✓ SSH连接成功
```

**如果提示输入密码**: 输入你的阿里云服务器密码。

**如果连接失败**:
- 检查网络连接
- 确认阿里云服务器是否正常运行
- 确认 IP 地址是否正确

---

## ☁️ 第二部分：阿里云服务器操作

### 步骤 6: 登录阿里云服务器

**在本机终端执行**:

```bash
ssh lvhe@8.140.21.235
```

**说明**: 如果提示输入密码，输入你的服务器密码。

登录成功后，你会看到命令提示符变化，类似：
```
lvhe@iZ2ze... ~$
```

---

### 步骤 7: 进入项目目录

**在阿里云服务器执行**:

```bash
cd /var/www/ai-tender-system
```

**验证**:
```bash
pwd
```

应该显示:
```
/var/www/ai-tender-system
```

---

### 步骤 8: 查看阿里云当前数据（可选）

**在阿里云服务器执行**:

```bash
# 检查数据库是否存在
ls -lh ai_tender_system/data/knowledge_base.db
```

**可能的结果**:

**情况A**: 文件存在
```
-rw-r--r-- 1 lvhe lvhe 512K Oct 30 15:20 ai_tender_system/data/knowledge_base.db
```

**情况B**: 文件不存在
```
ls: cannot access 'ai_tender_system/data/knowledge_base.db': No such file or directory
```

**说明**: 无论哪种情况都没关系，我们接下来会备份（如果存在）并上传新的。

---

### 步骤 9: 创建备份目录

**在阿里云服务器执行**:

```bash
mkdir -p ai_tender_system/data/db_backups
```

**说明**: 创建备份目录，用于保存旧数据库（如果存在）。

---

### 步骤 10: 备份现有数据库（如果存在）

**在阿里云服务器执行**:

```bash
if [ -f ai_tender_system/data/knowledge_base.db ]; then
    TIMESTAMP=$(date +%Y%m%d_%H%M%S)
    cp ai_tender_system/data/knowledge_base.db \
       ai_tender_system/data/db_backups/knowledge_base_${TIMESTAMP}.db
    echo "✓ 已备份到: knowledge_base_${TIMESTAMP}.db"
else
    echo "ℹ️  阿里云暂无数据库，跳过备份"
fi
```

**预期输出**（情况A - 有旧数据库）:
```
✓ 已备份到: knowledge_base_20251103_154520.db
```

**预期输出**（情况B - 无旧数据库）:
```
ℹ️  阿里云暂无数据库，跳过备份
```

---

### 步骤 11: 退出阿里云服务器

**在阿里云服务器执行**:

```bash
exit
```

**说明**: 退出 SSH 连接，回到本机终端。

---

## 🔄 第三部分：上传数据库（本机操作）

### 步骤 12: 上传数据库到阿里云

**在本机终端执行**:

```bash
scp ai_tender_system/data/knowledge_base.db \
    lvhe@8.140.21.235:/var/www/ai-tender-system/ai_tender_system/data/knowledge_base.db
```

**说明**:
- 如果提示输入密码，输入阿里云服务器密码
- 上传过程会显示进度条

**预期输出**:
```
knowledge_base.db                             100%  744KB   2.1MB/s   00:00
```

**上传成功标志**:
- 进度达到 100%
- 没有错误提示
- 返回到命令提示符

---

## ✅ 第四部分：验证和重启（阿里云操作）

### 步骤 13: 重新登录阿里云

**在本机终端执行**:

```bash
ssh lvhe@8.140.21.235
```

---

### 步骤 14: 进入项目目录

**在阿里云服务器执行**:

```bash
cd /var/www/ai-tender-system
```

---

### 步骤 15: 验证数据库已上传

**在阿里云服务器执行**:

```bash
ls -lh ai_tender_system/data/knowledge_base.db
```

**预期输出**:
```
-rw-r--r-- 1 lvhe lvhe 744K Nov  3 15:46 ai_tender_system/data/knowledge_base.db
```

**检查重点**:
- ✓ 文件大小约 744K（与本地一致）
- ✓ 修改时间是刚才上传的时间

---

### 步骤 16: 验证数据库完整性

**在阿里云服务器执行**:

```bash
sqlite3 ai_tender_system/data/knowledge_base.db "PRAGMA integrity_check;"
```

**预期输出**:
```
ok
```

**说明**:
- 如果输出 "ok"，说明数据库完整无损
- 如果输出其他内容，说明数据库可能损坏，需要重新上传

---

### 步骤 17: 查看数据统计

**在阿里云服务器执行**:

```bash
sqlite3 ai_tender_system/data/knowledge_base.db << 'SQL'
.mode column
.headers on
SELECT
    (SELECT COUNT(*) FROM companies) as 公司数,
    (SELECT COUNT(*) FROM documents) as 文档数,
    (SELECT COUNT(*) FROM resumes) as 简历数,
    (SELECT COUNT(*) FROM case_studies) as 案例数;
SQL
```

**预期输出**:
```
公司数      文档数      简历数      案例数
----------  ----------  ----------  ----------
2           0           1           1
```

**对比**: 这个数据应该和**步骤3**本地的数据一致！

---

### 步骤 18: 重启应用

**在阿里云服务器执行**:

```bash
sudo supervisorctl restart ai-tender-system
```

**说明**: 如果提示输入密码，输入你的服务器密码（sudo 权限）。

**预期输出**:
```
ai-tender-system: stopped
ai-tender-system: started
```

---

### 步骤 19: 检查应用状态

**在阿里云服务器执行**:

```bash
sleep 3
sudo supervisorctl status ai-tender-system
```

**预期输出**:
```
ai-tender-system                 RUNNING   pid 12345, uptime 0:00:03
```

**关键字**: `RUNNING` - 表示应用正在运行

**如果显示其他状态**:
- `STOPPED`: 应用未启动，需要检查日志
- `FATAL`: 应用启动失败，需要检查日志

---

### 步骤 20: 退出阿里云服务器

**在阿里云服务器执行**:

```bash
exit
```

---

## 🌐 第五部分：浏览器验证

### 步骤 21: 打开浏览器访问

在浏览器地址栏输入:

```
http://8.140.21.235
```

**预期**: 自动跳转到登录页面 `http://8.140.21.235/login`

---

### 步骤 22: 登录系统

- **用户名**: admin
- **密码**: admin123

点击"登录"按钮。

**预期**: 登录成功，跳转到系统主页。

---

### 步骤 23: 验证数据同步成功

#### 检查1: 公司管理

1. 在左侧菜单点击 "公司管理" 或 "知识库" → "公司库"
2. 应该看到 **2 家公司**

✅ 如果看到 2 家公司 → 同步成功！
❌ 如果看不到或数量不对 → 继续排查

---

#### 检查2: 简历库

1. 在左侧菜单点击 "知识库" → "简历库"
2. 应该看到 **1 份简历**

✅ 如果看到 1 份简历 → 同步成功！
❌ 如果看不到 → 继续排查

---

#### 检查3: 案例库

1. 在左侧菜单点击 "知识库" → "案例库"
2. 应该看到 **1 个案例**

✅ 如果看到 1 个案例 → 同步成功！
❌ 如果看不到 → 继续排查

---

## 🎉 完成

如果以上三项检查都通过，**恭喜你，数据库同步成功！**

---

## ❌ 故障排查

### 问题1: SSH 连接失败

**症状**: 步骤5 无法连接到阿里云

**解决**:
```bash
# 检查网络
ping 8.140.21.235

# 如果 ping 不通，检查:
# 1. 网络连接是否正常
# 2. 阿里云安全组是否开放 22 端口
# 3. 阿里云服务器是否正常运行
```

---

### 问题2: 上传失败

**症状**: 步骤12 上传进度卡住或报错

**解决**:
```bash
# 方法1: 重新上传
scp ai_tender_system/data/knowledge_base.db \
    lvhe@8.140.21.235:/var/www/ai-tender-system/ai_tender_system/data/knowledge_base.db

# 方法2: 使用 rsync（更可靠）
rsync -avz --progress ai_tender_system/data/knowledge_base.db \
    lvhe@8.140.21.235:/var/www/ai-tender-system/ai_tender_system/data/knowledge_base.db
```

---

### 问题3: 数据库完整性检查失败

**症状**: 步骤16 输出不是 "ok"

**解决**:
```bash
# 在阿里云服务器恢复备份
ssh lvhe@8.140.21.235
cd /var/www/ai-tender-system

# 查看备份列表
ls -lt ai_tender_system/data/db_backups/

# 恢复最新备份
LATEST_BACKUP=$(ls -t ai_tender_system/data/db_backups/knowledge_base_*.db | head -1)
cp "$LATEST_BACKUP" ai_tender_system/data/knowledge_base.db

# 然后回到本机，重新执行步骤12
```

---

### 问题4: 应用启动失败

**症状**: 步骤19 显示 STOPPED 或 FATAL

**解决**:
```bash
# 在阿里云服务器执行
ssh lvhe@8.140.21.235

# 查看错误日志
tail -50 /var/www/ai-tender-system/logs/supervisor-stderr.log

# 查看应用日志
tail -50 /var/www/ai-tender-system/logs/supervisor-stdout.log

# 尝试手动启动测试
cd /var/www/ai-tender-system
source venv/bin/activate
python3 main.py
# 查看是否有报错

# 如果是数据库问题，恢复备份后重试
```

---

### 问题5: 浏览器看不到数据

**症状**: 步骤23 数据数量不对

**解决**:
```bash
# 1. 清除浏览器缓存
# 按 Ctrl+Shift+R (Windows) 或 Command+Shift+R (Mac) 强制刷新

# 2. 重新登录
# 退出登录，重新登录系统

# 3. 在阿里云检查数据库
ssh lvhe@8.140.21.235
cd /var/www/ai-tender-system
sqlite3 ai_tender_system/data/knowledge_base.db << 'SQL'
SELECT COUNT(*) FROM companies;
SELECT COUNT(*) FROM resumes;
SELECT COUNT(*) FROM case_studies;
SQL

# 4. 如果数据库中有数据但页面看不到，重启应用
sudo supervisorctl restart ai-tender-system
```

---

## 📋 完整操作检查清单

复制此清单，逐项打勾确认：

### 本机操作
- [ ] 步骤1: 打开终端
- [ ] 步骤2: 进入项目目录
- [ ] 步骤3: 查看本地数据统计（记录数量）
- [ ] 步骤4: 检查数据库文件大小
- [ ] 步骤5: 测试 SSH 连接成功

### 阿里云操作（第一次）
- [ ] 步骤6: 登录阿里云服务器
- [ ] 步骤7: 进入项目目录
- [ ] 步骤8: 查看阿里云当前数据
- [ ] 步骤9: 创建备份目录
- [ ] 步骤10: 备份现有数据库
- [ ] 步骤11: 退出阿里云

### 本机上传
- [ ] 步骤12: 上传数据库到阿里云（进度100%）

### 阿里云操作（第二次）
- [ ] 步骤13: 重新登录阿里云
- [ ] 步骤14: 进入项目目录
- [ ] 步骤15: 验证数据库已上传（文件大小正确）
- [ ] 步骤16: 验证数据库完整性（输出 "ok"）
- [ ] 步骤17: 查看数据统计（与步骤3一致）
- [ ] 步骤18: 重启应用
- [ ] 步骤19: 检查应用状态（RUNNING）
- [ ] 步骤20: 退出阿里云

### 浏览器验证
- [ ] 步骤21: 打开浏览器访问系统
- [ ] 步骤22: 登录系统成功
- [ ] 步骤23: 验证公司数据（2家）
- [ ] 步骤23: 验证简历数据（1份）
- [ ] 步骤23: 验证案例数据（1个）

---

## 📞 需要帮助？

如果遇到问题，请提供：

1. **出错的步骤编号**
2. **完整的错误信息**（复制整段输出）
3. **执行的命令**
4. **截图**（如果是浏览器问题）

---

**创建日期**: 2025-11-03
**适用版本**: AI标书系统 v2.0
**预计时间**: 5-10 分钟
**难度等级**: ⭐⭐ (简单)
