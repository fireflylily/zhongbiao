# 标书宝 5.0 小程序集成文档

## 概述

本文档描述了标书宝 5.0 升级后，微信小程序需要对接的后端 API 接口及数据结构变化。

---

## 一、API 接口列表

### 基础接口（已有）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/mp/auth/login` | POST | 微信登录 |
| `/api/mp/auth/profile` | POST | 更新用户资料 |
| `/api/mp/risk/upload` | POST | 上传招标文件并分析 |
| `/api/mp/risk/status/<task_id>` | GET | 查询任务状态（支持轮询） |
| `/api/mp/risk/result/<task_id>` | GET | 获取分析结果 |
| `/api/mp/risk/history` | GET | 历史任务列表 |
| `/api/mp/risk/delete/<task_id>` | DELETE | 删除任务 |

### V5.0 新增接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/mp/risk/upload-response/<task_id>` | POST | 上传应答文件 |
| `/api/mp/risk/reconcile/<task_id>` | POST | 启动双向对账 |
| `/api/mp/risk/reconcile/<task_id>` | GET | 获取对账结果/进度 |
| `/api/mp/risk/export/<task_id>` | GET | 导出 Excel 报告 |

---

## 二、V5.0 新增接口详情

### 2.1 上传应答文件

**请求**
```
POST /api/mp/risk/upload-response/<task_id>
Content-Type: multipart/form-data
Authorization: Bearer <token>

FormData:
  - file: 应答文件 (PDF/Word)
  - filename: 原始文件名（可选）
```

**前置条件**
- 任务状态必须为 `completed`（招标分析已完成）

**响应**
```json
{
  "success": true,
  "data": {
    "task_id": "uuid-xxx",
    "response_file_path": "/path/to/file",
    "response_file_name": "投标应答.docx",
    "message": "应答文件上传成功，可启动对账"
  }
}
```

**小程序调用示例**
```javascript
// 上传应答文件
uploadResponseFile(taskId, filePath) {
  return new Promise((resolve, reject) => {
    wx.uploadFile({
      url: `${BASE_URL}/api/mp/risk/upload-response/${taskId}`,
      filePath: filePath,
      name: 'file',
      header: {
        'Authorization': `Bearer ${getToken()}`
      },
      formData: {
        filename: filePath.split('/').pop()
      },
      success: (res) => {
        const data = JSON.parse(res.data);
        if (data.success) {
          resolve(data.data);
        } else {
          reject(new Error(data.message));
        }
      },
      fail: reject
    });
  });
}
```

---

### 2.2 启动双向对账

**请求**
```
POST /api/mp/risk/reconcile/<task_id>
Authorization: Bearer <token>
Content-Type: application/json

{} // 无需请求体
```

**前置条件**
- 任务状态为 `completed` 或 `reconcile_completed`
- 已上传应答文件

**响应**
```json
{
  "success": true,
  "data": {
    "task_id": "uuid-xxx",
    "status": "reconciling",
    "message": "对账任务已启动"
  }
}
```

**小程序调用示例**
```javascript
// 启动对账
startReconcile(taskId) {
  return request({
    url: `/api/mp/risk/reconcile/${taskId}`,
    method: 'POST'
  });
}
```

---

### 2.3 获取对账结果/进度

**请求**
```
GET /api/mp/risk/reconcile/<task_id>
Authorization: Bearer <token>
```

**响应**
```json
{
  "success": true,
  "data": {
    "task_id": "uuid-xxx",
    "status": "reconcile_completed",  // reconciling | reconcile_completed | reconcile_failed
    "reconcile_progress": 100,
    "reconcile_step": "对账完成",
    "reconcile_summary": {
      "total": 10,
      "compliant": 6,
      "non_compliant": 2,
      "partial": 1,
      "unknown": 1
    },
    "risk_items": [...],  // 包含合规状态的风险项
    "reconcile_results": [...],  // 详细对账结果
    "response_file_name": "投标应答.docx"
  }
}
```

**轮询示例**
```javascript
// 轮询对账进度
async pollReconcileStatus(taskId) {
  const poll = async () => {
    const res = await request({
      url: `/api/mp/risk/reconcile/${taskId}`,
      method: 'GET'
    });

    if (res.data.status === 'reconciling') {
      // 更新进度 UI
      this.setData({
        reconcileProgress: res.data.reconcile_progress,
        reconcileStep: res.data.reconcile_step
      });
      // 继续轮询
      setTimeout(poll, 2000);
    } else {
      // 对账完成或失败
      this.handleReconcileComplete(res.data);
    }
  };

  poll();
}
```

---

### 2.4 导出 Excel 报告

**请求**
```
GET /api/mp/risk/export/<task_id>?include_reconcile=true
Authorization: Bearer <token>
```

**参数**
- `include_reconcile`: 是否包含对账结果（默认 true）

**响应**
- 成功：返回 Excel 文件流
- 失败：返回 JSON 错误信息

**小程序调用示例**
```javascript
// 下载 Excel 报告
downloadExcel(taskId) {
  wx.downloadFile({
    url: `${BASE_URL}/api/mp/risk/export/${taskId}`,
    header: {
      'Authorization': `Bearer ${getToken()}`
    },
    success: (res) => {
      if (res.statusCode === 200) {
        // 打开文档
        wx.openDocument({
          filePath: res.tempFilePath,
          fileType: 'xlsx',
          success: () => console.log('打开文档成功')
        });
      }
    }
  });
}
```

---

## 三、数据结构变化

### 3.1 风险项 (RiskItem) 新增字段

| 字段 | 类型 | 说明 | V5新增 |
|------|------|------|--------|
| `requirement` | string | 核心要求 | |
| `risk_type` | string | 风险类型：废标项/★条款/隐性风险 | |
| `risk_level` | string | 风险等级：high/medium/low | |
| `location` | string | 章节来源 | |
| `original_text` | string | 招标文件原文 | ✅ |
| `position_index` | string | 位置索引（页码） | ✅ |
| `todo_action` | string | 具体 Todo 操作 | ✅ |
| `deep_analysis` | string | 问题深度解析 | ✅ |
| `compliance_status` | string | 合规状态（对账后） | ✅ |
| `compliance_note` | string | 合规备注（对账后） | ✅ |
| `match_score` | float | 匹配得分 0-1（对账后） | ✅ |
| `response_text` | string | 应答文件相关内容（对账后） | ✅ |

### 3.2 合规状态枚举

```javascript
const COMPLIANCE_STATUS = {
  'compliant': '符合',      // 🟢 绿色
  'non_compliant': '不符合', // 🔴 红色
  'partial': '部分符合',     // 🟡 黄色
  'unknown': '未知'         // ⚪ 灰色
};
```

### 3.3 任务状态枚举

```javascript
const TASK_STATUS = {
  // 基础状态
  'pending': '等待中',
  'parsing': '解析文档',
  'analyzing': '分析中',
  'completed': '分析完成',
  'failed': '分析失败',

  // V5 对账状态
  'reconciling': '对账中',
  'reconcile_completed': '对账完成',
  'reconcile_failed': '对账失败'
};
```

---

## 四、UI 升级建议

### 4.1 风险卡片升级

**原有展示：**
- 风险要求
- 风险类型标签

**V5.0 新增展示：**
```
┌─────────────────────────────────────────────┐
│ [高风险] [废标项]              合规状态: 🟢  │  ← 新增合规徽章
├─────────────────────────────────────────────┤
│ 📍 第三章 评标办法 P45                      │  ← 位置索引
│                                             │
│ 核心要求：投标人须具有AAA信用等级证书        │
│                                             │
│ ▼ 招标原文（可折叠）                         │  ← 新增
│   "投标人应提供近三年的企业信用等级证明..."   │
│                                             │
│ ▼ 深度解析                                  │  ← 新增
│   此条款为强制性资质要求，如不满足将...       │
│                                             │
│ ✅ 建议操作                                 │  ← 新增 Todo
│   联系资质部确认AAA证书有效期，扫描件备用     │
│                                             │
│ ▼ 对账结果（对账后显示）                     │  ← 新增
│   应答内容："我司具有AAA级信用证书..."       │
│   评估：符合要求，证书在有效期内             │
└─────────────────────────────────────────────┘
```

### 4.2 新增「上传应答」入口

分析完成后，在结果页显示上传应答区域：

```
┌─────────────────────────────────────────────┐
│ 🔄 双向对账                                 │
│                                             │
│ 上传您的投标应答文件，自动检查是否满足       │
│ 招标要求，一键生成合规报告                   │
│                                             │
│        ┌──────────────────────┐             │
│        │   📄 上传应答文件     │             │
│        │   支持 PDF/Word      │             │
│        └──────────────────────┘             │
│                                             │
│ 已上传：投标应答.docx                        │  ← 上传后显示
│        ┌──────────────────────┐             │
│        │   🔍 开始对账        │             │
│        └──────────────────────┘             │
└─────────────────────────────────────────────┘
```

### 4.3 对账进度展示

```
┌─────────────────────────────────────────────┐
│ 正在对账...                                 │
│                                             │
│ ████████████░░░░░░░░  60%                   │
│                                             │
│ 正在对账第 6/10 项...                        │
└─────────────────────────────────────────────┘
```

### 4.4 对账汇总展示

```
┌─────────────────────────────────────────────┐
│ 对账结果汇总                                │
│                                             │
│   🟢 符合      6 项                         │
│   🔴 不符合    2 项                         │
│   🟡 部分符合  1 项                         │
│   ⚪ 待确认    1 项                         │
│                                             │
│ ┌──────────────────────┐                    │
│ │  📥 导出 Excel 报告   │                    │
│ └──────────────────────┘                    │
└─────────────────────────────────────────────┘
```

---

## 五、完整流程示例

```javascript
// 1. 上传招标文件（已有）
const uploadResult = await uploadBidFile(filePath);
const taskId = uploadResult.task_id;

// 2. 轮询分析进度（已有）
await pollAnalysisStatus(taskId);

// 3. 获取分析结果（已有，但字段已增强）
const analysisResult = await getAnalysisResult(taskId);
// analysisResult.risk_items 现在包含 original_text, todo_action, deep_analysis 等新字段

// 4. 【新】上传应答文件
await uploadResponseFile(taskId, responseFilePath);

// 5. 【新】启动对账
await startReconcile(taskId);

// 6. 【新】轮询对账进度
await pollReconcileStatus(taskId);

// 7. 【新】获取对账结果
const reconcileResult = await getReconcileResult(taskId);
// reconcileResult.risk_items 现在包含 compliance_status, compliance_note 等字段
// reconcileResult.reconcile_summary 包含汇总统计

// 8. 【新】导出 Excel
await downloadExcel(taskId);
```

---

## 六、错误处理

### 常见错误码

| 状态码 | 错误信息 | 处理建议 |
|--------|----------|----------|
| 400 | 请先完成招标文件分析 | 等待分析完成后再上传应答 |
| 400 | 请先上传应答文件 | 先调用 upload-response 接口 |
| 400 | 不支持的文件格式 | 仅支持 PDF/DOC/DOCX |
| 400 | 文件大小不能超过 20MB | 压缩或分割文件 |
| 404 | 任务不存在或无权访问 | 检查 task_id 和登录状态 |
| 500 | 启动对账失败 | 检查后端日志，可能是 AI 服务异常 |

---

## 七、版本兼容性

- 后端 API 完全向后兼容
- 旧版小程序可正常使用基础功能
- 新功能需要小程序升级后使用
- V5 新增字段在旧数据中为空值，前端需做空值处理

---

## 八、联系方式

如有问题，请联系后端开发人员或查看后端日志：

```bash
# 查看风险分析模块日志
tail -f logs/risk_analyzer.log

# 查看 API 请求日志
tail -f logs/web.log
```
