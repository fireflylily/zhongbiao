# AI标书系统环境变量配置示例
# 复制此文件为 .env 并填入你的配置值

# ===================
# API配置
# ===================
# 始皇API密钥 (必填)
DEFAULT_API_KEY=sk-your-api-key-here

# API端点 (可选，默认值如下)
API_ENDPOINT=https://api.oaipro.com/v1/chat/completions

# 模型名称 (可选，默认值如下)
MODEL_NAME=gpt-5

# 最大令牌数 (可选，默认1000)
MAX_TOKENS=1000

# API超时时间，秒 (可选，默认30)
API_TIMEOUT=30

# ===================
# 多模型API配置
# ===================
# OpenAI GPT-4o-mini配置
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_API_ENDPOINT=https://api.oaipro.com/v1/chat/completions
OPENAI_MAX_TOKENS=1000
OPENAI_TIMEOUT=30

# 联通元景大模型配置
# 基于官方API文档的正确配置
# 获取方式：访问 https://maas.ai-yuanjing.com 注册账户并创建应用
ACCESS_TOKEN=your-access-token-here
UNICOM_BASE_URL=https://maas-api.ai-yuanjing.com/openapi/compatible-mode/v1
UNICOM_MODEL_NAME=deepseek-v3
UNICOM_MAX_TOKENS=1000
UNICOM_TIMEOUT=30

# 联通元景多模型选项（系统支持以下6个模型）:
#
# 通用对话模型:
# - deepseek-v3: DeepSeek-V3模型，平衡性能与速度
#
# 高性能写作模型（推荐用于标书）:
# - qwen3-235b-a22b: 通义千问3-235B，最大参数模型，文本质量最高
# - ernie-4.5-300b-a47b: 百度文心4.5-300B，中文商务写作专长
# - glm-z1-rumination-32b-0414: 智谱GLM思考版，逻辑推理强
#
# 专业功能模型:
# - yuanjing-70b-chat: 联通自研70B对话模型，政企场景优化
# - deepseek-v3-functioncall: DeepSeek函数调用版，结构化输出强
#
# 注意：所有模型共享相同的API配置和访问限制（试用账号每分钟5次）

# ===================
# Web服务配置
# ===================
# 服务绑定地址 (可选，默认0.0.0.0)
WEB_HOST=0.0.0.0

# 服务端口 (可选，默认8082)
WEB_PORT=8082

# 调试模式 (可选，默认True)
DEBUG=True

# 会话密钥 (可选，建议修改)
SECRET_KEY=ai-tender-system-2025

# ===================
# 安全配置
# ===================
# 最大上传文件大小，MB (可选，默认50)
MAX_UPLOAD_SIZE=50

# 允许的文件类型 (可选)
ALLOWED_EXTENSIONS=pdf,doc,docx,txt

# ===================
# Embeddings API配置 (用于文本向量化)
# ===================
# 嵌入模型API密钥 (默认使用OPENAI_API_KEY)
# EMBEDDING_API_KEY=sk-your-embedding-api-key-here

# 嵌入模型API端点 (默认使用OPENAI_API_ENDPOINT)
# EMBEDDING_API_ENDPOINT=https://api.oaipro.com/v1

# 嵌入模型名称 (可选，默认text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-small
# 可选模型:
# - text-embedding-3-small: 1536维,性价比高 (推荐)
# - text-embedding-3-large: 3072维,精度更高
# - text-embedding-ada-002: 1536维,经典模型

# 嵌入API超时时间，秒 (可选，默认30)
# EMBEDDING_API_TIMEOUT=30

# ===================
# 使用说明
# ===================
# 1. 复制此文件为 .env
# 2. 填入你的API密钥等配置
# 3. 重启应用以加载新配置
# 4. 请确保 .env 文件不要提交到版本控制系统
# 5. Embeddings API会自动使用OPENAI_API_KEY,无需单独配置