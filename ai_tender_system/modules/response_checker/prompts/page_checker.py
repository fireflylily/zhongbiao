#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
页码和索引检查提示词
"""

PAGE_CHECK_SYSTEM = """你是一位专业的投标文件审核专家。
你的职责是检查文档的目录和页码情况。"""

PAGE_CHECK_PROMPT = """请检查以下投标文档内容的目录和页码情况。

## 文档内容
{document_content}

## 检查要点

### 1. 目录页检查
- 文档开头是否包含目录页
- 目录格式是否规范

### 2. 页码检查
- 是否有页码标记
- 页码是否连续

## 输出要求

请判断以下2个检查项的状态：

1. **包含目录页**
2. **页码连续正确**

## 输出格式（严格JSON）
```json
{{
  "items": [
    {{
      "name": "包含目录页",
      "status": "符合/不符合/无法判断",
      "detail": "具体说明",
      "location": "",
      "suggestion": "建议（不符合时）"
    }},
    {{
      "name": "页码连续正确",
      "status": "符合/不符合/无法判断",
      "detail": "",
      "location": "",
      "suggestion": ""
    }}
  ]
}}
```

## 判断标准
- **符合**：文档中存在目录页/页码连续
- **不符合**：缺少目录页/页码不连续或缺失
- **无法判断**：无法从文本确定

请只返回JSON，不要添加其他说明。"""


INDEX_CHECK_SYSTEM = """你是一位专业的投标文件审核专家。
你的职责是检查文档的索引表情况。"""

INDEX_CHECK_PROMPT = """请检查以下投标文档内容的索引表情况。

## 文档内容
{document_content}

## 检查要点

### 1. 索引表存在性
- 是否包含文件索引表/资料清单
- 索引表是否列出了所有附件

### 2. 索引页码正确性
- 索引中标注的页码是否与实际位置相符
- 是否有页码错误或遗漏

## 输出要求

请判断以下2个检查项的状态：

1. **包含索引表**
2. **索引页码与实际一致**

## 输出格式（严格JSON）
```json
{{
  "items": [
    {{
      "name": "包含索引表",
      "status": "符合/不符合/无法判断",
      "detail": "具体说明",
      "location": "",
      "suggestion": "建议（不符合时）"
    }},
    {{
      "name": "索引页码与实际一致",
      "status": "符合/不符合/无法判断",
      "detail": "",
      "location": "",
      "suggestion": ""
    }}
  ]
}}
```

## 判断标准
- **符合**：索引表存在且页码正确
- **不符合**：缺少索引表或页码不正确
- **无法判断**：无法从文本确定

请只返回JSON，不要添加其他说明。"""
