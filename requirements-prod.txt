# AI标书系统生产环境依赖 - 轻量级版本
# 移除了PyTorch、transformers等大型依赖,改用API

# ===================
# Web框架
# ===================
Flask==2.3.3
Flask-CORS==4.0.0
Flask-WTF==1.2.1
Werkzeug==2.3.7
gunicorn==21.2.0

# ===================
# HTTP客户端
# ===================
requests==2.31.0
openai>=1.0.0  # OpenAI SDK (用于LLM和Embeddings API)

# ===================
# 文档解析相关
# ===================
PyMuPDF>=1.23.0
python-docx>=0.8.11
pdfplumber>=0.9.0
openpyxl>=3.1.0

# ===================
# 文本处理和分块
# ===================
langchain>=0.1.0
langchain-text-splitters>=0.0.1
langchain-community>=0.0.1  # LangChain社区集成
tiktoken>=0.5.0
jieba>=0.42.1
aiohttp>=3.8.0  # 异步HTTP客户端

# ===================
# 向量搜索
# ===================
faiss-cpu>=1.7.4  # 向量检索引擎,轻量级CPU版本
numpy>=1.24.0     # 数值计算基础库

# ===================
# 文本清洗和处理
# ===================
beautifulsoup4>=4.12.0
lxml>=4.9.0
regex>=2023.6.3

# ===================
# 日志
# ===================
loguru>=0.7.0

# ===================
# 已移除的大型依赖(改用API):
# ===================
# torch>=2.0.0              (~2GB) - 移除,改用Embeddings API
# transformers>=4.30.0      (~500MB) - 移除,改用Embeddings API
# sentence-transformers>=2.2.2 (~100MB) - 移除,改用Embeddings API
# scikit-learn>=1.3.0       - 移除,向量相似度计算用numpy即可
# celery>=5.3.0             - 移除,暂不需要异步任务队列
# redis>=4.5.0              - 移除,暂不需要缓存
